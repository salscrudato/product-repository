rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    // Internal-only application: Allow all read/write access
    // No authentication required
    match /{allPaths=**} {
      allow read, write: if true;
    }
  }
}